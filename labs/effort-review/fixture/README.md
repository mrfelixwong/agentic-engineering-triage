# Category normalization service

This service turns provider category strings into support queues. Provider
compatibility requirements live in `packet/provider-category-contract.md`.

Run the visible checks with:

- `.venv/bin/pytest -q`
- `.venv/bin/python -m eval.run`
